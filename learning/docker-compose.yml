services:
  hashcards:
    build: .
    container_name: hashcards
    restart: unless-stopped
    volumes:
      - hashcards_data:/data
    networks:
      - proxy_net

volumes:
  hashcards_data:

networks:
  proxy_net:
    external: true
